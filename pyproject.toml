[project]
name = "zxtoolbox"
version = "0.1.0"
description = "some used tool collections"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "paramiko>=3.5.1",
    "psutil>=7.0.0",
    "py-cpuinfo>=9.0.0",
    "pynvml>=12.0.0",
]

[project.scripts]
zxtoolbox = "main:main"